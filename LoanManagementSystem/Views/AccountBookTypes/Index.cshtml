@model IEnumerable<Data.Models.Accounts.sdtoAccountBookType>

@{
    ViewBag.Title = "Index";
    
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AccountBookType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AccountBookType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AccountBookTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AccountBookTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AccountBookTypeId })
        </td>
    </tr>
}

</table>
